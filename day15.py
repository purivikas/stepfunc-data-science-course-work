import csv


def build_model(csv_file_in , contain_numeric=False):
    ''' Takes in the name of a csv file , and builds a model on 1R. '''
    with open('filename_', 'r') as o_file:

        listfile = csv.reader(o_file, delimiter=';')
        prev_attribute = attribute

        for attribute  in listfile.split() :
            get_optimal_rule(colume_in)
         return

        return
    return rule_out


def test_build(filename_ , string_):
    ''' takes in a csv file conataining test data, and a rule and returns the accurate of the rule on the dataset. '''

    build_model(filename_) :



    return

def get_optimal_rule(colume_in):
    '''return the best rule in in given column'''

    for outcome in listfile[:-1]:
        if outcome == prev_outcome:
            list_outcome.append(outcome)
            prev_outcome = outcome
        else
            prev_outcome = outcome

    return

    return (optimal_rule, accuracy )